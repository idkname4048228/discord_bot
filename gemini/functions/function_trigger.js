const { setAlarmClock, getCurrentTimestamp } = require("./time/clock")

const functions = {
    setAlarmClock: setAlarmClock,
    getCurrentTimestamp: getCurrentTimestamp
}

module.exports = functions